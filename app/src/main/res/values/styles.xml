<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryColorDark</item>
        <item name="colorAccent">@color/accentColor</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <!--<item name="navigationBarColor">@color/primaryColorDark</item>-->
        <item name="colorControlHighlight">@color/controlHighlightColor</item>
        <item name="colorControlActivated">@color/controlHighlightColor</item>
        <item name="colorControlNormal">@color/controlHighlightColor</item>
        <item name="colorButtonNormal">@color/buttonNormalColor</item>
    </style>


    <style name="AppTheme" parent="AppTheme.Base"></style>

</resources>
